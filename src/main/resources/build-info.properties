# Build information - заполняется автоматически при сборке Maven
app.version=${project.version}
app.name=${project.artifactId}
app.build.timestamp=${maven.build.timestamp}
app.build.time=${maven.build.timestamp}
app.build.java.version=${java.version}
app.build.spring.boot.version=${project.parent.version}

