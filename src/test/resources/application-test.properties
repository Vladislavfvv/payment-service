# Test Configuration
spring.application.name=payment-service

# MongoDB Configuration for tests
#spring.data.mongodb.uri=mongodb://localhost:27017/paymentdb

# Embedded MongoDB Configuration  #port 0 = automatically selection
spring.data.mongodb.uri=mongodb://localhost:0/testdb 

## Optional: Specify MongoDB version
#spring.mongodb.embedded.version=4.4.13

# Disable indexes
spring.data.mongodb.auto-index-creation=false

# Disable Liquibase for tests
spring.liquibase.enabled=false

# Kafka Configuration for tests (optional, can be mocked)
spring.kafka.bootstrap-servers=localhost:9092

# Disable version Mongo
spring.mongodb.embedded.version=4.0.12

# ????????? Spring Security ??? ??????
spring.security.enabled=false
# ????????? Spring Security ?????????
spring.autoconfigure.exclude=org.springframework.boot.autoconfigure.security.servlet.SecurityAutoConfiguration
